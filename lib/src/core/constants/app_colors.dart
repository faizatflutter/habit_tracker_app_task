import 'package:flutter/material.dart';

class AppStyles {
  static const buttonColor = Colors.blue;
  static const whiteColor = Colors.white;
  static const errorColor = Colors.red;
  static const successColor = Colors.green;
  static const borderColor = Color(0xFFBDBDBD);
  static Color shadowColor = Colors.grey.withOpacity(0.08);

  static const darkTextColor = Colors.black54;
  static var lightTextColor = Colors.grey[700];
}
